<Page
    x:Class="AppServiceClient.MainPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:AppServiceClient"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <Grid Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        <StackPanel Margin="10" Orientation="Horizontal">
            <TextBlock Text="Enter the text" Width="Auto"></TextBlock>
            <TextBox x:Name="TxtStatus" Margin="10,0,0,0" Width="200" KeyDown="TxtStatus_OnKeyDown"></TextBox>
        </StackPanel>
        <StackPanel Grid.Row="1" Margin="10" Orientation="Horizontal">
            <TextBlock x:Name="TxtSendStatus" Margin="5" Foreground="Red"/>
            <TextBlock Text="@VinothRajendran" Margin="5" Foreground="Brown"/>
        </StackPanel>
    </Grid>
</Page>
